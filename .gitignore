# === データ / PII（絶対に公開しない） ===
vault/
docs/
scripts/casper_context.md
scripts/casper_rag_index.json
scripts/*.jsonl
scripts/assets/
scripts/diagrams/
# === 秘密情報 ===
.env
*.env
*.token
*token*.txt
# === python / 一時 ===
__pycache__/
*.pyc
.venv/
*.log
scripts/threads/
scripts/.casper_secret
scripts/.casper_write_token
scripts/casper_embed_index.json
scripts/casper_embed_index.json.tmp.*
scripts/.casper_user_mcp.json
scripts/.casper_user_mcp.json.tmp
scripts/roster_cache.json
scripts/events_store.jsonl
scripts/.events_cursor*

# === Casper 秘密/設定ファイル(全 .casper_ 系を遮断) ===
scripts/.casper_*
*vimeo_token*
*.casper_aurora
CASPER_WRITE_TOKEN.txt.bak*
scripts/casper_outbox.jsonl
scripts/casper_trace.jsonl
queue/casper_alerts.jsonl
queue/casper_alerts_dispatch_state.json
scripts/cases_pending.jsonl
scripts/.casper_dropbox_token
scripts/casper_embed.db
scripts/casper_notify_state.json

# === 実行時状態 / push購読(秘匿: 端末のpush認証キー含む) 2026-07-15 ===
scripts/push_subs.json
scripts/dm_notify_state.json
scripts/notify_prefs.json
scripts/breaker.json

# 索引の件数サイドカー(実行時生成物・cmd_498)
scripts/casper_embed_index.json.meta.json

# 雲へ出た内容の帳簿(社の情報・0600・git管理外)
scripts/casper_cloud_ledger.jsonl
scripts/casper_embed_reindex.jsonl

# ★台帳(宛先・席の禁足・雲の可否・座席状態)は履歴へ入れる(殿御裁可2026-08-24)。
#   秘密は含まぬ(実読確認済: token/key/password 無し・IP/ポート/モデル名/フラグのみ)。
#   機構が書き換えるゆえ常時dirtyになるが、「誰がいつ宛先を変えたか」の履歴が
#   無いことの方が重い(2026-08-21「書き換えたのに座席が変わらぬ」混乱の因)。
!scripts/casper_endpoints.env
